[project]
name = "codebase_graphrag"
version = "0.1.0"
description = "Local Knowledge Graph RAG for codebases"
requires-python = ">=3.11"

[tool.pyright]
venvPath = "."
venv = "venv"
extraPaths = [
    ".",
    "venv/lib/python3.13/site-packages"
]
